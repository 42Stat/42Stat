import { css } from '@emotion/react';

// todo: this is temp component
export const LoadingSpinner = () => {
  return (
    <div
      css={css`
        color: white;
      `}
    >
      loading...
    </div>
  );
};
